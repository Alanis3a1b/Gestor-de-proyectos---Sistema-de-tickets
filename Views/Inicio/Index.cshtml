@{
    Layout = "_LoginLayout";
}

@* Este es el Index para la pantalla de inicio de autenticacion, unida junto con el _LoginLayout *@



<head>
    <link rel="stylesheet" href="/css/site.css" />
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<form asp-action="ValidarUsuario">
    <!--Lineas para capturar la informacion-->

   <div class="Fondo">
        <div class="container-fluid">
            <div class="container-fluid login-container text-center mt-5 pt-5 justify-content-center align-content-center"
                 style="max-width: 500px; margin-left: auto; margin-right: auto; padding-bottom: 20px; border-radius: 8px; margin-top: 20%; padding-top: 20px;">


                <div class="img-fluid mx-auto d-block mb-3" style="width: 30%">
                    <img src="/images/logo.svg" />
                </div>

                <div class="m-5 text-white">
                    <h1> Bienvenido al centro de soporte HelpHup </h1>
                </div>


                <div class="col-sm-2 text-white">
                    <label>Usuario</label>
                </div>
                <div class="col-sm-4" style="width: 100%; padding: 10px; margin-bottom: 10px;  border-radius: 4px;">
                    @Html.TextBox("usuario", "", new { @class = "form-control", style = "background-color: #1e0029; color: white;" })
                </div>

                <div class="col-sm-2 text-white">
                    <label>Contraseña</label>
                </div>
                <div class="col-sm-4" style="width: 100%; padding: 10px; margin-bottom: 10px; border-radius: 4px;">
                    @Html.TextBox("contrasenya", "", new { @class = "form-control", type = "password", style = "background-color: #1e0029; color: white;" })
                </div>

                <!--Comprobacion por si hay algun mensaje enviado proveniente del backend-->
                @if (!string.IsNullOrEmpty(ViewBag.Mensaje))
                {
                    <div class="alert alert-danger" role="alert">
                        @ViewBag.Mensaje
                    </div>
                }

                <input type="submit" class="btn btn-light" style="width: 100%; padding: 10px; margin-bottom: 10px; border: 1px solid #1e0029; border-radius: 4px;" value="Iniciar Sesión" />

            </div>
        </div>
   </div>


</form>
